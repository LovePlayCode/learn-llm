# 3-27 复习记录

## 复习内容：Transformer 模型架构（第2天间隔检索）

### 掌握的内容

- **seq2seq 两个问题**：信息瓶颈（解码器只拿到压缩摘要）+ 顺序处理（RNN 无法并行）。经纠正后掌握。
- **注意力机制 QKV 计算流程**：Q×K 点积 → 注意力分数 → softmax → 对 V 加权求和。初次遗忘，提示后完整回忆出来。
- **并行化**：预填充阶段可并行处理所有输入 token，输出阶段因自回归无法并行。一次答对。
- **横向连接（注意力 vs RAG）**：注意力解决模型内部"已有信息看不到"的问题，RAG 解决"信息根本不存在于模型中"的问题。

### 需要加强

- **RNN 顺序 vs 并行**：初次回答时说反了（说成"seq2seq只能并行解析"），说明对 RNN 顺序处理的特性记忆不牢，需加强。
- **注意力机制解决信息瓶颈的方式**：描述不够精确，需牢记：让解码器在生成每个输出 token 时可以**直接访问每一个输入 token**，按需关注最相关的部分。
- **QKV 计算流程**：第一次没能回忆出完整步骤，说明尚未内化，需多检索几次。

---

## 下次复习安排

| 内容 | 下次复习时间 | 间隔 | 题型 |
|------|-------------|------|------|
| Transformer 模型架构 | 4月2日 | 第7天 | 5道题，含跨章节关联题 |
